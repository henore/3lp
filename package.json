{
  "name": "app-websites",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "astro dev --root apps/simple-cbt-i --host 127.0.0.1",
    "build": "astro build --root apps/simple-cbt-i",
    "check": "astro check --root apps/simple-cbt-i",
    "verify": "node scripts/verify.mjs",
    "dev:fast": "astro dev --root apps/simple-fasting-timer --host 127.0.0.1 --port 4322",
    "build:fast": "astro build --root apps/simple-fasting-timer",
    "check:fast": "astro check --root apps/simple-fasting-timer",
    "verify:fast": "node scripts/verify.mjs build",
    "dev:sla": "astro dev --root apps/smart-live-assist --host 127.0.0.1 --port 4323",
    "build:sla": "astro build --root apps/smart-live-assist",
    "check:sla": "astro check --root apps/smart-live-assist",
    "verify:sla": "node scripts/verify.mjs build-sla"
  },
  "dependencies": {
    "@rollup/rollup-linux-x64-gnu": "^4.63.3",
    "astro": "^5.13.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@axe-core/playwright": "^4.10.2",
    "playwright": "^1.55.0",
    "typescript": "^5.9.2"
  }
}
